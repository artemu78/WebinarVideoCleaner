repos:
-   repo: local
    hooks:
    -   id: run-tests
        name: Run Unit Tests
        entry: ./tests.sh
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
